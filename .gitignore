
# Local planning + private manuscript-derived notes (do not publish)
tasks/
.session_log*.md

# Python
__pycache__/
*.pyc

# Local dev preview config
.claude/

# Capture tooling artifacts
tools/node_modules/
tools/shots/
tools/package-lock.json

# BodyParts3D source archive (547 MB) — fetched by build/anatomy/fetch_source.py,
# never committed. Only the derived GLBs under docs/assets/anatomy/ ship.
build/anatomy/.cache/
